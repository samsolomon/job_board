<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-brand"><%= link_to "Security Jobs", root_path %></div>
    <ul class="nav navbar-nav">
      <li><%= link_to "All Jobs", root_path %></li>
        <% Category.all.each do |category| %>
        <li><%= link_to category.name, jobs_path(category: category.name) %></li>
      <% end %>
    </ul>

    <ul class="nav navbar-nav navbar-right">
    <% if signed_in? %>
      <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'navbar-text navbar-link' %>
      <%= link_to "Post New Job", new_job_path, class: "btn btn-primary navbar-btn" %>
    <% else %>
      <%= link_to 'Sign In', new_user_session_path, class: "navbar-text navbar-link" %>
      <%= link_to 'Post New Job', new_user_registration_path, class: "btn btn-primary navbar-btn" %>
    <% end %>
    </ul>
  </div>
</nav>
